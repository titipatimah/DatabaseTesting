# Database Configuration for Supabase PostgreSQL

# URL koneksi database Supabase (HAPUS TANDA ? DI AKHIR)
db.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres

# Username database
db.username=postgres.lykkasznatjlkoscbnib

# Password database (GANTI DENGAN PASSWORD ASLI DARI SUPABASE)
db.password=39yFeHmFgnfCutUU

# JDBC Driver class
db.driver=org.postgresql.Driver

# Connection Pool Settings (optional)
db.pool.initialSize=5
db.pool.maxTotal=20
db.pool.maxIdle=10
db.pool.minIdle=5

# Test configuration
db.test.on.startup=true

db.connection.timeout=30000
db.socket.timeout=30000
db.ssl=true
db.sslmode=require